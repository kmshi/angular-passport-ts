{
  "private": true,
  "dependencies": {
  },
  "scripts": {
    "compile":"tsc -p ./src/client && tsc -p ./src/server && ./node_modules/.bin/r.js -o bundle.build.js"
  },
  "devDependencies": {
    "requirejs": "~2.1.22"
  }
}
