{
  "private": true,
  "dependencies": {
  },
  "scripts": {
    "build":"tsc -p ./src/client/pc && tsc -p ./src/server && ./node_modules/.bin/r.js -o pcbundle.build.js"
  },
  "devDependencies": {
    "requirejs": "~2.1.22"
  }
}
